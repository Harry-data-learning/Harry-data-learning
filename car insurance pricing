SELECT 
    p.policy_id,
    p.exposure,
    p.vehicle_type,
    p.driver_age,
    p.gender,
    p.zipcode,
    p.annual_mileage,
    c.num_claims,
    c.total_loss_amount
FROM policy_data p
LEFT JOIN claims_data c
ON p.policy_id = c.policy_id
WHERE p.exposure > 0;

